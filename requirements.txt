# Core drone control
pymavlink==2.4.41

# Vision & ML
numpy>=1.17,<1.20           # 1.20.0 dropped Python 3.6
onnxruntime>=1.8,<1.14      # 1.14.0 dropped Python 3.6
onnx>=1.6,<1.12             # required by onnxruntime.quantization; 1.12+ dropped Python 3.6
six>=1.16                   # required by onnxruntime quantization calibrate module
opencv-python>=4.5,<4.7     # 4.7+ has no cp36 wheels on Windows

# ArcFace detector
torch>=1.7.1,<1.10          # 1.10+ dropped cp36 wheels on Windows
torchvision>=0.8.2,<0.11    # must match torch version (0.8.2=torch1.7.1, 0.10.1=torch1.9.1)
facenet-pytorch>=2.5.0      # requires torch; compatible with Python 3.6 and torch 1.7-1.9

# Supporting
Pillow>=7.1,<10.0           # 10.0.0 dropped Python 3.6
protobuf>=3.12,<4.0         # 4.0 dropped Python 3.6
flatbuffers>=1.12,<3.0      # newer versions (23+, 25+) dropped Python 3.6
fastcrc>=0.2.1
lxml>=4.5,<5.0              # 5.0.0 dropped Python 3.6
psutil>=5.8,<6.0            # 6.0.0 dropped Python 3.6
future>=0.18,<1.0
